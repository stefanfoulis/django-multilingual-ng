[buildout]
versions = versions
develop = 
    ../../django-multilingual-ng
parts =
    python
    django
eggs =
    Django
    django-multilingual-ng

[django]
recipe = djangorecipe
project = testproject
settings = settings
eggs = ${buildout:eggs}
test =
	testproject

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}

[versions]
Django = 1.2.5

